<main>
<div class="container">  
 <div class="row">
  <div class="col-md-8 offset-md-1">

  <h1>New Book</h1>
  <% if @book.errors.any? %>
  <%= @book.errors.count %>errors prohibited this book from being saved:
  <% @book.errors.full_messages.each do |message| %>
    <%= message %>
  <% end %>
  <% end %>
  <%= form_with model:@book, local:true do |f| %>

  <h4>Titl</h4>
  <%= f.text_field :title %>

  <h4>Body</h4>
  <%= f.text_field :body %>

  <%= f.submit 'CreateBook', class: "btn btn-secondary" "btn btn-success" %>

  <% end %>
 </table>
 </div> 
</div>  
</div> 
</main>