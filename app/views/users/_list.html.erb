<p id="notice"><%= notice %></p>
<p id="alert"><%= alert %></p>

  <h2>User info</h2>
  <%= attachment_image_tag user, :profile_image, :fill, 100, 100, format: 'jpeg', fallback: '/assets/no_image.jpg' %>
  <p>name <%= user.name %></p>
  <p>Introduction  <%= user.introduction %></p>
  <% if user.id == current_user.id %>
    <p><%= link_to "プロフィール編集", edit_user_path(user) %></p>
  <% end %>
  
  <h1>New Book</h1>
  <% if @book.errors.any? %>
  <%= @book.errors.count %>errors prohibited this book from being saved:
  <% @book.errors.full_messages.each do |message| %>
    <%= message %>
  <% end %>
  <% end %>
  <%= form_with model:@book, local:true do |f| %>

  <h4>Title</h4>
  <%= f.text_field :title %>

  <h4>Body</h4>
  <%= f.text_field :body %>
  <h4></h4>
  <%= f.submit 'CreateBook', class: "btn btn-secondary" "btn btn-success" %>

  <% end %>
  </table>

